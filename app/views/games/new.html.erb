<h1>New</h1>

<div class="d-flex justify-content-between  m-5">
<% @letters.each do |letter| %>
  <div class="p-3 btn btn-primary"><%= letter %></div>
<% end %>
</div>

<form action="TODO" method="TODO" data-turbo="false">
  <!-- ... -->
</form>
<form action="/score" method="post" data-turbo="false" class="m-5">
  <div class="mb-3">
    <label for="formFile" class="form-label">What is the longest word you can find?</label>
    <div class="input-group">
      <input autofocus type="text" name="word" class="form-control">
      <div class="input-group-append">
        <button class="btn btn-outline-secondary" type="submit">Play</button>
      </div>
      <input id="token" name="token" type="hidden" value=<%= @letters.join("") %> />
    </div>
  </div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
</form>
